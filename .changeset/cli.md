---
"@streamd/cli": minor
---

The `--allow-dangerous-meta-html` flag is no longer part of the CLI.
Math rendering and syntax highlighting are component overrides in the
library API; the CLI emits default HTML only.
