---
"@streamd/react-native": minor
---

Default RN component per token type, snake_case `components={}`
override map matching `TokenTypeValue`. `CodeBlock` renders
`meta.highlight` as per-segment `<Text>` nodes. New
`@streamd/react-native/streaming` subpath with platform-split
`<Words>` (native uses the core `Animated` API; web uses CSS
keyframes) and 16 animation presets.
